#include <stdio.h>

int main()
{
    // char s;
    // printf("Enter a character: \n");
    // scanf(" %c", &s);  // Note the space before %c to consume newline
    // printf("You entered: %c\n", s);

    int a =5;
    int b=2;
    printf("%f",(float)a/b);
    printf("\n %d",100/10*10);
    
    return 0;
}
